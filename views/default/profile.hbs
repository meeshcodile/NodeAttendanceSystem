<!-- Body -->

<div class="col-sm-8 text-left">
    <!-- Current date -->
    <div class="wel">
        <h1>Welcome</h1>
        <form action="/user/signIn/{{user._id}}" method="POST">
        
            <div class="btn btn-success ">
                <button type="submit" id="btnControl1" class="btn btn-lg meesh" style="color: #000;font-weight:bolder">SignIN Today
                </button>
            </div>
        </form>
        <form action="/user/signOut/{{user._id}}" method="POST">
        
            <div class="btn btn-warning ">
                <button type="submit" id="btnControl2" class="btn btn-lg meesh" style="color: #000;font-weight:bolder">SignOut Today
                    </button>
    
            </div>
        </form>
        
        <p id="date"></p>


        <hr>
        <p>nHub Foundation is a non-governmental and not-for-profit organization that works to enhance policy advocacy,
            gender inclusiveness, Technology for all, infrastructure/capacity building as regards Employability and
            Entrepreneurship, innovation and every aspect of social impact</p>
        

        {{!-- saving each individuL TOPICS --}}
        {{!-- <form action="/user/save/{{user._id}}" method="POST">
        <div class="form-group">
            <label for="recipient-name" class="form-control-label">Hospital</label>
            <select class="default-select select2 form-control" name="topic">
                <option data-display="Hospital">Select Your Project Topic</option>
                {{#each topic}}
                <option>{{projectTopic}}</option>
                {{/each}}
               
            </select>
        </div>
        <button type="submit">save</button>
        </form> --}}

        <form action="/user/addTopic/{{user._id}}" method="POST">
            <h2 class="h3">Add Project Topics</h2>
            <input type="text" name="projectTopic"><br>
            <button type="submit">Save</button>
        
        </form>
        {{!-- {{topic}} --}}
        <hr>
    </div>
</div>

</div>
</div>